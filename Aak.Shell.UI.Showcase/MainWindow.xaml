<MetroWindow
    x:Class="Aak.Shell.UI.Showcase.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:collection="clr-namespace:Aak.Shell.UI.Showcase.ViewModels.Collection"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Aak.Shell.UI.Showcase"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shell="clr-namespace:Aak.Shell.UI.Showcase.Shell"
    xmlns:styles="clr-namespace:Aak.Shell.UI.Showcase.Styles"
    xmlns:vm="clr-namespace:Aak.Shell.UI.Showcase.ViewModels"
    Title="Aak.Shell.UI.Showcase"
    Width="1280"
    Height="720"
    d:DataContext="{d:DesignInstance Type=vm:WorkSpaceViewModel}"
    Activated="MetroWindow_Activated"
    Icon="pack://application:,,,/icon.ico"
    IsShowIcon="False"
    IsShowTitle="False"
    OnMaximizedPadding="0"
    RenderOptions.BitmapScalingMode="Fant"
    ResizeMode="CanResizeWithGrip"
    SnapsToDevicePixels="True"
    TextOptions.TextFormattingMode="Display"
    TextOptions.TextRenderingMode="ClearType"
    UseLayoutRounding="True"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <MetroWindow.DataContext>
        <vm:WorkSpaceViewModel />
    </MetroWindow.DataContext>
    <MetroWindow.LeftWindowCommands>
        <Grid Margin="4,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Viewbox
                Width="16"
                Height="16"
                Margin="4,2,8,0"
                IsHitTestVisible="False">
                <Rectangle
                    Width="16"
                    Height="16"
                    Style="{StaticResource WindowIconRectangle}" />
            </Viewbox>
            <Menu Grid.Column="1" Margin="0,2,0,0">
                <MenuItem Header="View(_V)">
                    <MenuItem
                        Header="{Binding StyleSelector.Title}"
                        IsCheckable="True"
                        IsChecked="{Binding StyleSelector.IsVisible, Mode=TwoWay}" />
                </MenuItem>
                <MenuItem Header="Themes(_T)">
                    <MenuItem
                        Command="{Binding ThemeSwitchCommand, Mode=OneWay}"
                        CommandParameter="{x:Static local:AakVS2019Blue.Default}"
                        Header="{Binding Path=Name, Source={x:Static local:AakVS2019Blue.Default}}"
                        IsCheckable="True">
                        <MenuItem.IsChecked>
                            <MultiBinding Converter="{StaticResource AakTwoThemeIsEqualsConverter}" Mode="OneWay">
                                <MultiBinding.Bindings>
                                    <Binding Path="CurrentTheme" />
                                    <Binding Source="{x:Static local:AakVS2019Blue.Default}" />
                                </MultiBinding.Bindings>
                            </MultiBinding>
                        </MenuItem.IsChecked>
                    </MenuItem>
                    <MenuItem
                        Command="{Binding ThemeSwitchCommand, Mode=OneWay}"
                        CommandParameter="{x:Static local:AakVS2019Dark.Default}"
                        Header="{Binding Path=Name, Source={x:Static local:AakVS2019Dark.Default}}"
                        IsCheckable="True">
                        <MenuItem.IsChecked>
                            <MultiBinding Converter="{StaticResource AakTwoThemeIsEqualsConverter}" Mode="OneWay">
                                <MultiBinding.Bindings>
                                    <Binding Path="CurrentTheme" />
                                    <Binding Source="{x:Static local:AakVS2019Dark.Default}" />
                                </MultiBinding.Bindings>
                            </MultiBinding>
                        </MenuItem.IsChecked>
                    </MenuItem>
                    <MenuItem
                        Command="{Binding ThemeSwitchCommand, Mode=OneWay}"
                        CommandParameter="{x:Static local:AakVS2019Light.Default}"
                        Header="{Binding Path=Name, Source={x:Static local:AakVS2019Light.Default}}"
                        IsCheckable="True">
                        <MenuItem.IsChecked>
                            <MultiBinding Converter="{StaticResource AakTwoThemeIsEqualsConverter}" Mode="OneWay">
                                <MultiBinding.Bindings>
                                    <Binding Path="CurrentTheme" />
                                    <Binding Source="{x:Static local:AakVS2019Light.Default}" />
                                </MultiBinding.Bindings>
                            </MultiBinding>
                        </MenuItem.IsChecked>
                    </MenuItem>
                    <MenuItem
                        Command="{Binding ThemeSwitchCommand, Mode=OneWay}"
                        CommandParameter="{x:Static local:AakVS2022Blue.Default}"
                        Header="{Binding Path=Name, Source={x:Static local:AakVS2022Blue.Default}}"
                        IsCheckable="True">
                        <MenuItem.IsChecked>
                            <MultiBinding Converter="{StaticResource AakTwoThemeIsEqualsConverter}" Mode="OneWay">
                                <MultiBinding.Bindings>
                                    <Binding Path="CurrentTheme" />
                                    <Binding Source="{x:Static local:AakVS2022Blue.Default}" />
                                </MultiBinding.Bindings>
                            </MultiBinding>
                        </MenuItem.IsChecked>
                    </MenuItem>
                    <MenuItem
                        Command="{Binding ThemeSwitchCommand, Mode=OneWay}"
                        CommandParameter="{x:Static local:AakVS2022Dark.Default}"
                        Header="{Binding Path=Name, Source={x:Static local:AakVS2022Dark.Default}}"
                        IsCheckable="True">
                        <MenuItem.IsChecked>
                            <MultiBinding Converter="{StaticResource AakTwoThemeIsEqualsConverter}" Mode="OneWay">
                                <MultiBinding.Bindings>
                                    <Binding Path="CurrentTheme" />
                                    <Binding Source="{x:Static local:AakVS2022Dark.Default}" />
                                </MultiBinding.Bindings>
                            </MultiBinding>
                        </MenuItem.IsChecked>
                    </MenuItem>
                    <MenuItem
                        Command="{Binding ThemeSwitchCommand, Mode=OneWay}"
                        CommandParameter="{x:Static local:AakVS2022Light.Default}"
                        Header="{Binding Path=Name, Source={x:Static local:AakVS2022Light.Default}}"
                        IsCheckable="True">
                        <MenuItem.IsChecked>
                            <MultiBinding Converter="{StaticResource AakTwoThemeIsEqualsConverter}" Mode="OneWay">
                                <MultiBinding.Bindings>
                                    <Binding Path="CurrentTheme" />
                                    <Binding Source="{x:Static local:AakVS2022Light.Default}" />
                                </MultiBinding.Bindings>
                            </MultiBinding>
                        </MenuItem.IsChecked>
                    </MenuItem>
                </MenuItem>
            </Menu>
            <Border
                Grid.Column="2"
                Margin="5,2,0,0"
                VerticalAlignment="Top"
                IsHitTestVisible="False">
                <Border.Style>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="Background" Value="Transparent" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsActive, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SystemColorsWindowPanel}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock
                    Margin="6,4,6,4"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontWeight="Bold"
                    Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                    TextTrimming="CharacterEllipsis">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsActive, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Value="False">
                                    <Setter Property="TextElement.Foreground" Value="{DynamicResource EnvironmentMainWindowInactiveCaptionText}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Border>
        </Grid>
    </MetroWindow.LeftWindowCommands>
    <MetroWindow.RightWindowCommands>
        <StackPanel Orientation="Horizontal">
            <ComboBox
                Width="160"
                Margin="4,6"
                DisplayMemberPath="Name"
                SelectedItem="{Binding CurrentTheme, Mode=TwoWay}">
                <x:Static Member="local:AakVS2019Blue.Default" />
                <x:Static Member="local:AakVS2019Dark.Default" />
                <x:Static Member="local:AakVS2019Light.Default" />
                <x:Static Member="local:AakVS2022Blue.Default" />
                <x:Static Member="local:AakVS2022Dark.Default" />
                <x:Static Member="local:AakVS2022Light.Default" />
            </ComboBox>
            <Button
                Width="45"
                Height="32"
                VerticalAlignment="Top"
                BorderBrush="{Binding Background, RelativeSource={RelativeSource Self}}"
                Content="✨" />
        </StackPanel>
    </MetroWindow.RightWindowCommands>
    <Grid x:Name="root">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockingManager
            x:Name="dockingManager"
            ActiveContent="{Binding ActiveDocument, Mode=TwoWay, Converter={StaticResource AakActiveDocumentConverter}}"
            ActiveContentChanged="DockingManager_ActiveContentChanged"
            AnchorablesSource="{Binding Anchorables}"
            DocumentTitleTemplate="{StaticResource AakDocumentWellTitleTemplate}"
            DocumentsSource="{Binding DocumentViews}">
            <DockingManager.LayoutItemTemplateSelector>
                <styles:AakTemplateSelector>
                    <styles:AakTemplateSelector.AakDocumentWellTemplate>
                        <DataTemplate DataType="{x:Type shell:AakDocumentWell}">
                            <ScrollViewer HorizontalScrollBarVisibility="Auto">
                                <Grid Margin="15">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="15" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock
                                        FontSize="16"
                                        FontWeight="Bold"
                                        Text="{Binding Title}" />
                                    <ContentPresenter Grid.Row="2" Content="{Binding View}" />
                                </Grid>
                            </ScrollViewer>
                        </DataTemplate>
                    </styles:AakTemplateSelector.AakDocumentWellTemplate>
                    <styles:AakTemplateSelector.AakToolWellTemplate>
                        <DataTemplate DataType="{x:Type shell:AakToolWell}">
                            <ContentPresenter Content="{Binding View}" />
                        </DataTemplate>
                    </styles:AakTemplateSelector.AakToolWellTemplate>
                    <styles:AakTemplateSelector.AakCollectionTemplate>
                        <DataTemplate DataType="{x:Type shell:AakCollection}">
                            <ScrollViewer HorizontalScrollBarVisibility="Auto">
                                <Grid Margin="15">
                                    <StackPanel>
                                        <TextBlock FontSize="24" Text="{Binding Title}" />
                                        <ItemsControl ItemsSource="{Binding Views, Mode=OneWay}" />
                                    </StackPanel>
                                </Grid>
                            </ScrollViewer>
                        </DataTemplate>
                    </styles:AakTemplateSelector.AakCollectionTemplate>
                </styles:AakTemplateSelector>
            </DockingManager.LayoutItemTemplateSelector>
            <DockingManager.LayoutItemContainerStyleSelector>
                <styles:AakStyleSelector>
                    <styles:AakStyleSelector.AakDocumentWellStyle>
                        <Style TargetType="{x:Type LayoutItem}">
                            <Setter Property="Title" Value="{Binding Model.Title}" />
                            <Setter Property="ToolTip" Value="{Binding Model.ToolTip}" />
                            <Setter Property="CloseCommand" Value="{Binding Model.CloseCommand}" />
                        </Style>
                    </styles:AakStyleSelector.AakDocumentWellStyle>
                    <styles:AakStyleSelector.AakToolWellStyle>
                        <Style TargetType="{x:Type LayoutAnchorableItem}">
                            <Setter Property="Title" Value="{Binding Model.Title}" />
                            <Setter Property="CanHide" Value="{Binding Model.CanHide, Mode=TwoWay}" />
                            <Setter Property="Visibility" Value="{Binding Model.IsVisible, Mode=TwoWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter={x:Static Visibility.Hidden}}" />
                            <Setter Property="IsSelected" Value="{Binding Model.IsSelected, Mode=TwoWay}" />
                            <Setter Property="IsActive" Value="{Binding Model.IsActive, Mode=TwoWay}" />
                        </Style>
                    </styles:AakStyleSelector.AakToolWellStyle>
                </styles:AakStyleSelector>
            </DockingManager.LayoutItemContainerStyleSelector>
            <LayoutRoot>
                <LayoutPanel Orientation="Horizontal">
                    <LayoutDocumentPaneGroup>
                        <LayoutDocumentPane />
                    </LayoutDocumentPaneGroup>
                </LayoutPanel>
            </LayoutRoot>
        </DockingManager>
        <StatusBar
            x:Name="mainStatusBar"
            Grid.Row="1"
            Background="{x:Null}"
            Foreground="{DynamicResource SystemColorsWindowText}">
            <StatusBarItem Margin="2,0,0,0" Content="Ready" />
            <StatusBarItem
                Margin="0,0,20,0"
                HorizontalAlignment="Right"
                Content="{Binding ActiveDocument, Converter={StaticResource AakViewElementToStringConverter}}"
                ContentStringFormat="Selected: {0}" />
        </StatusBar>
    </Grid>
</MetroWindow>
